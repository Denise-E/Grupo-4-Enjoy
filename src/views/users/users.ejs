<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head")  %> 
<body>
    <%- include("../partials/header")  %>
    <h2>Usuarios: <%= user.name %> </h2>
    <form action="/users/update" method="POST">
        <input type="name" name="name" id="name" placeholder="name">
        <input type="password" name="password" id="password">
        <button type="submit"> Usuario</button>
    </form>
    <section>
        <% if (user.avatar != null) { %>
            <figure>
                <img src="/avatars/<%= user.avatar %>" alt="photo <%= user.email %>">
            </figure>
        <% } else { %>
            <figure>
                <img src="/assets/avatar.png" alt="photo <%= user.email %>">
            </figure>
        <% } %>
        <form action="/users/upload/avatar" enctype="multipart/form-data" method="post">
            <input type="file" name="avatar" id="avatar">
            <button type="submit">Update Avatar</button>
        </form>
    </section>
</body>
</html>